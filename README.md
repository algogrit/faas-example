FAAS Sample
-----------

Fetching:

    $ go get -u github.com/gauravagarwalr/faas-example

Building app:

    $ dep ensure

    $ make build

Run locally:

    $ npm install

    $ npm install -g aws-sam-local

    $ npm run dev
